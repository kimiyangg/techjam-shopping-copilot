__pycache__/
*.py[cod]
.DS_Store
.env
data/catalog.jsonl
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log

# Local-only: Claude Code config and release downloads.
.claude/
catalog.jsonl.gz
SHA256SUMS

# Derived from the catalog by `python3 -m starter.build_index` (~25 MB) and
# not committed. The trained alignment matrix (data/catalog.alignment.npz,
# ~64 KB) IS committed: it is a model artifact the submission must ship to
# be reproducible, per docs/submission_rules.md.
data/catalog.semantic.npz
